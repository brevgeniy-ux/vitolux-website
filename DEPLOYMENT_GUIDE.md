# 🚀 Гайд по деплою VitoluxUA

## 📋 Содержание
1. [SQL Миграции](#sql-миграции)
2. [Загрузка API](#загрузка-api)
3. [Настройка админ-панели](#настройка-админ-панели)
4. [Настройка фронтенда](#настройка-фронтенда)
5. [Telegram уведомления](#telegram-уведомления)

---

## 📊 SQL Миграции

### Шаг 1: Выполнить миграции в phpMyAdmin

1. Откройте phpMyAdmin для базы данных `shmfjhml_vitolux`
2. Перейдите на вкладку **SQL**
3. Скопируйте и выполните содержимое файла `database/migrations.sql`

Это создаст таблицы:
- `orders` - для хранения заказов
- `settings` - для настроек сайта
- Обновит таблицу `products` (добавит поля если их нет)

---

## 🔧 Загрузка API

### Шаг 2: Загрузить index.php

1. Откройте DirectAdmin File Manager
2. Перейдите в `/domains/vitoluxua.com/public_html/`
3. Создайте папку `api` если её нет
4. Загрузите файл `api/index.php` в `/domains/vitoluxua.com/public_html/api/`

### Шаг 3: Создать папку для загрузок

1. В DirectAdmin File Manager создайте папку:
   ```
   /domains/vitoluxua.com/public_html/uploads/products/
   ```
2. Установите права доступа `755` на папку `uploads` и `products`

### Шаг 4: Проверить .htaccess

Убедитесь, что в `/domains/vitoluxua.com/public_html/.htaccess` есть правила для API:

```apache
# API routing
RewriteCond %{REQUEST_URI} ^/api/
RewriteRule ^api/(.*)$ api/index.php [L,QSA]
```

---

## ⚙️ Настройка админ-панели

### Шаг 5: Настроить переменные окружения

1. В папке `admin/` создайте файл `.env.production`:
```env
VITE_API_URL=https://vitoluxua.com/api
```

2. Соберите админ-панель:
```bash
cd admin
npm install
npm run build
```

3. Загрузите содержимое папки `admin/dist/` на Vercel:
   - Подключите репозиторий к Vercel
   - Укажите корневую папку: `admin`
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Установите переменную окружения: `VITE_API_URL=https://vitoluxua.com/api`

### Шаг 6: Проверить vercel.json

Убедитесь, что файл `admin/vercel.json` существует и содержит:
```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

---

## 🌐 Настройка фронтенда

### Шаг 7: Настроить переменные окружения

1. В папке `frontend/` создайте файл `.env.production`:
```env
VITE_API_URL=https://vitoluxua.com/api
```

2. Соберите фронтенд:
```bash
cd frontend
npm install
npm run build
```

3. Загрузите содержимое папки `frontend/dist/` в:
   ```
   /domains/vitoluxua.com/public_html/
   ```

### Шаг 8: Настроить .htaccess для фронтенда

В `/domains/vitoluxua.com/public_html/.htaccess` должны быть правила:

```apache
# API routing (должно быть первым)
RewriteCond %{REQUEST_URI} ^/api/
RewriteRule ^api/(.*)$ api/index.php [L,QSA]

# Frontend routing (для React Router)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} !^/api/
RewriteRule . /index.html [L]
```

---

## 📱 Telegram уведомления

### Шаг 9: Настроить Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram:
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните полученный **Bot Token**

2. Получите Chat ID:
   - Напишите боту любое сообщение
   - Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   - Найдите `chat.id` в ответе

3. Войдите в админ-панель и перейдите в **Налаштування**
4. Заполните:
   - **Bot Token**: токен от BotFather
   - **Chat ID**: ваш chat ID
   - Включите переключатель **Увімкнути сповіщення**
5. Нажмите **Надіслати тестове повідомлення** для проверки

---

## ✅ Проверка работы

### Проверка API:
1. Откройте: `https://vitoluxua.com/api/products`
2. Должен вернуться JSON с товарами

### Проверка админ-панели:
1. Откройте: `https://vitoluxua-admin.vercel.app`
2. Войдите с учетными данными администратора
3. Проверьте работу всех разделов

### Проверка фронтенда:
1. Откройте: `https://vitoluxua.com`
2. Проверьте работу каталога, корзины, оформления заказа

### Проверка Telegram:
1. Оформите тестовый заказ на сайте
2. Проверьте получение уведомления в Telegram

---

## 🔒 Безопасность

1. **Пароли**: Убедитесь, что пароли в БД захешированы через `password_hash()`
2. **Токены**: В продакшене рекомендуется использовать JWT вместо сессий
3. **CORS**: Проверьте настройки CORS в `api/index.php`
4. **Загрузка файлов**: Ограничьте типы и размеры загружаемых файлов

---

## 🐛 Решение проблем

### API возвращает 404:
- Проверьте путь к `index.php`
- Проверьте `.htaccess` правила
- Убедитесь, что mod_rewrite включен

### CORS ошибки:
- Проверьте заголовки в `api/index.php`
- Убедитесь, что домены указаны правильно

### Telegram не работает:
- Проверьте Bot Token и Chat ID
- Убедитесь, что бот активен
- Проверьте логи ошибок на сервере

### Сессии не работают:
- Проверьте права на папку сессий
- Убедитесь, что `session_start()` вызывается правильно

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи ошибок PHP на сервере
2. Консоль браузера (F12)
3. Network вкладку в DevTools
