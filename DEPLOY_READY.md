# ✅ Проект готов к деплою!

## 📦 Что было сделано:

1. ✅ **Frontend собран** - `frontend/dist/` готов к загрузке
2. ✅ **Admin панель собрана** - `admin/dist/` готова к загрузке
3. ✅ **Созданы скрипты деплоя** - `deploy.sh` и `deploy-server.sh`
4. ✅ **Подготовлены инструкции** - `DEPLOY_STEPS.md` и `DEPLOY_INSTRUCTIONS.md`
5. ✅ **Создан .env.production** - шаблон для production окружения
6. ✅ **Создан .htaccess** - конфигурация для Apache

## 🚀 Быстрый деплой (рекомендуется)

### Вариант 1: Через Git на сервере

```bash
# 1. Подключитесь к серверу
ssh shmfjhml@rocket-da6.hostsila.org -p 22

# 2. Клонируйте репозиторий
cd /home/shmfjhml/domains/vitoluxua.com/
git clone https://github.com/brevgeniy-ux/vitolux-website.git temp_repo

# 3. Скопируйте backend
cp -r temp_repo/backend/* public_html/
cd public_html

# 4. Настройте .env
cp .env.example .env
nano .env  # Замените DB_PASSWORD на реальный пароль

# 5. Установите зависимости и настройте Laravel
composer install --optimize-autoloader --no-dev
php artisan key:generate
php artisan migrate --force
php artisan db:seed
php artisan storage:link
php artisan config:cache
php artisan route:cache
php artisan view:cache
chmod -R 755 storage bootstrap/cache

# 6. Очистите временные файлы
cd ..
rm -rf temp_repo
```

### Вариант 2: Загрузка через SFTP

1. **Backend**: Загрузите содержимое `backend/` в `public_html/`
2. **Frontend**: Загрузите содержимое `frontend/dist/` в `public_html/`
3. **Admin**: Загрузите содержимое `admin/dist/` в `public_html/admin/`

Затем выполните шаги 4-5 из Варианта 1.

## 📋 Чеклист перед деплоем:

- [ ] У вас есть пароль от базы данных MySQL
- [ ] У вас есть доступ к SSH/SFTP серверу
- [ ] Frontend собран (`frontend/dist/` существует)
- [ ] Admin собран (`admin/dist/` существует)
- [ ] Вы знаете пароль от хостинга

## 🔑 Важные данные для .env:

```env
DB_DATABASE=shmfjhml_vitolux
DB_USERNAME=shmfjhml_vitolux
DB_PASSWORD=ВАШ_ПАРОЛЬ_ОТ_БД
```

## 🌐 После деплоя проверьте:

1. **API**: https://vitoluxua.com/api/health
2. **Главная**: https://vitoluxua.com
3. **Админка**: https://vitoluxua.com/admin
4. **Вход**: admin@vitoluxua.com / admin123

## 📚 Документация:

- **Подробная инструкция**: `DEPLOY_STEPS.md`
- **Полное руководство**: `DEPLOY_INSTRUCTIONS.md`
- **Скрипты деплоя**: `deploy.sh` и `deploy-server.sh`

## ⚠️ Важно:

1. **Измените пароль админа** после первого входа!
2. **Настройте реальные контакты** в `.env`
3. **Проверьте права доступа** к `storage` и `bootstrap/cache`
4. **Убедитесь что PHP 8.3** установлен на сервере

## 🆘 Если что-то пошло не так:

1. Проверьте логи: `storage/logs/laravel.log`
2. Проверьте права: `chmod -R 755 storage bootstrap/cache`
3. Проверьте .env файл
4. Проверьте подключение к БД

---

**Готово к деплою! Удачи! 🚀**
