import { AboutClient } from "./AboutClient";

export const metadata = {
  title: "About us | Vitolux",
  description: "About Vitolux: generators, welding, heat pumps, stabilizers, compressors, service support.",
};

export default function AboutPage() {
  return <AboutClient />;
}
