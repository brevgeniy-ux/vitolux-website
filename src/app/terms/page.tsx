import { TermsClient } from "./TermsClient";

export const metadata = {
  title: "Terms & Conditions | Vitolux",
  description: "Vitolux terms of use and warranties.",
};

export default function TermsPage() {
  return <TermsClient />;
}
